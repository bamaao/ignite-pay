[Unit]
Description=Ignite Pay — Channel Service (Hub)
After=network-online.target ignite-pay-hub-registry.service
Wants=network-online.target

[Service]
Type=simple
User=ignite-pay
Group=ignite-pay
ExecStart=/opt/ignite-pay/channel-hub /etc/ignite-pay/channel-hub.toml
WorkingDirectory=/opt/ignite-pay
Environment=RUST_LOG=info
Restart=on-failure
RestartSec=5
LimitNOFILE=65536

NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/lib/ignite-pay /var/log/ignite-pay
BindPaths=/etc/ignite-pay
PrivateTmp=true

StandardOutput=journal
StandardError=journal
SyslogIdentifier=ignite-pay-channel-hub

[Install]
WantedBy=multi-user.target
