[Unit]
Description=Ignite Pay — DID Registry
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=ignite-pay
Group=ignite-pay
ExecStart=/opt/ignite-pay/did-registry /etc/ignite-pay/did-registry.toml
WorkingDirectory=/opt/ignite-pay
Environment=RUST_LOG=info
Restart=on-failure
RestartSec=5
LimitNOFILE=65536

NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/lib/ignite-pay /var/log/ignite-pay
BindPaths=/etc/ignite-pay
PrivateTmp=true

StandardOutput=journal
StandardError=journal
SyslogIdentifier=ignite-pay-did-registry

[Install]
WantedBy=multi-user.target
