[package]
name = "ignite-pay-solana"
version = "0.1.0"
edition = "2021"
description = "Solana integration for Ignite Pay: SPL Account Compression, Session Keys, and on-chain payments"

[dependencies]
# Solana SDK & client
solana-sdk = "2.1"
solana-client = "2.1"

# SPL programs
spl-account-compression = "1"
spl-noop = "1"
spl-token = "7"
spl-associated-token-account = "6"

# Serialization
borsh = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Encoding
bs58 = "0.5"

# Error handling
thiserror = "2"
anyhow = "1"

# Logging
tracing = "0.1"

# Async runtime
tokio = { version = "1", features = ["full"] }

# Persistence
sled = "0.34"

# HTTP client (for Helius DAS API)
reqwest = { version = "0.12", features = ["json"] }

[dev-dependencies]
tempfile = "3"
