import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts without error', (WidgetTester tester) async {
    // Smoke test placeholder — full widget tests require
    // mocking RustLib and services.
    expect(true, isTrue);
  });
}
